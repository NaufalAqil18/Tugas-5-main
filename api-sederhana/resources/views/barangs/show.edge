<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Barang</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin: 20px; }
        .actions { margin-top: 20px; }
        .delete-button { color: white; background-color: red; border: none; padding: 5px 10px; cursor: pointer; }
    </style>
</head>
<body>
    <h2>Detail Barang</h2>
    <p><strong>Nama:</strong> {{ barang.nama }}</p>
    <p><strong>Harga:</strong> {{ barang.harga }}</p>
    <p><strong>Stok:</strong> {{ barang.stok }}</p>

    <div class="actions">
        <a href="/barangs/{{ barang.id }}/edit">Edit</a>
        <form action="/barangs/{{ barang.id }}" method="POST" style="display: inline;">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="delete-button" onclick="return confirm('Yakin ingin menghapus?')">Hapus</button>
        </form>
    </div>
    <a href="/barangs">Kembali ke Daftar Barang</a>
</body>
</html>
